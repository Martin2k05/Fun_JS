/* ARRAYS O ARREGLOS

un array es una estructurade datos que se utiliza para almacenar una coleccion ordenada de elementos.

*/

let numeros = []; // Declaracion de un array vacio.

numeros = [15,80,650,50.43,-10];
console.log(numeros); // Se imprime por consola todo el array
console.log(numeros[3]); // Se imprime por consola el 4to elemento que pertenece al indice 3.